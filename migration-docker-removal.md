# Docker removal note

Docker is not required for this Django + Neo4j + Render deployment (Render uses its own build process and Neo4j Aura is managed/cloud-hosted). Safe to remove Dockerfile and docker-compose.yml.
